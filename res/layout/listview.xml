<TableLayout android:layout_width="fill_parent"
	android:layout_height="fill_parent" xmlns:android="http://schemas.android.com/apk/res/android"
	android:id="@+id/listview_tbl" android:background="@drawable/stripe_repeat">

	<ListView android:id="@id/android:list" android:layout_width="fill_parent"
		android:layout_height="wrap_content"></ListView>

	<TextView android:layout_height="wrap_content" android:id="@id/android:empty"
		android:text="@string/msg_add_stock" android:layout_width="fill_parent"  android:padding="8sp"  android:textSize="22sp"></TextView>

</TableLayout>
